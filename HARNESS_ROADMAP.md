# Quorum Harness Roadmap

> 这是**追踪用**的精简路线图。完整方案与分级评审见
> [`AGENT-HARNESS-ARCHITECTURE-REVIEW.md`](./AGENT-HARNESS-ARCHITECTURE-REVIEW.md)。
> 最后更新：2026-08-03。

## 目标（一句话）

Quorum 的“评审大脑”（AI 投资人怎么打分/吵/给结论）已经不错；本路线图修的是**外围管道（harness）**——让一次评审**启动可靠、崩了不丢、别人偷不走、卡住能恢复**，并逐步让评审**更有据可查**。

两个术语：
- **worker**：后台真正跑评审的进程（网页只是显示）。
- **Temporal**：一个“让长流程崩了能自动接着跑”的外部系统（要钱、要学、要接）。**结论：P0–P4 都不需要它，能拖到 P5 再决定。**

## 现状快照

**P0（安全）+ P1（刷新不丢）+ §9 小收尾 已全部上线。** 到这里 Quorum 是个结实、能交的版本。

## 阶段

| 阶段 | 目标（大白话） | 好处 | 成本/风险 | 状态 | PR |
|---|---|---|---|---|---|
| **P0 安全** | 别人偷看评审、任务丢失、伪造登录、跨用户读数据 | 更安全 | 小 | ✅ 已上线 | #1 #3 |
| **P1 刷新不丢** | 刷新/断网后对话完整恢复（含待回应的 founder 表单） | 明显 | 小 | ✅ 已上线 | #4 |
| **§9 小收尾** | 防误删进行中的评审、非法输入干净报错、日志不打密码 | 少踩坑 | 很小 | ✅ 已上线 | #5 |
| **P2 崩溃不丢** | worker 半路重启，“正在等你回应”的评审还能接着跑 | 部署/重启时评审不卡死 | **大**（要改引擎；分水岭） | ⏳ 待决定 | — |
| **P3 文档/证据层** | BP 不再截断前 6000 字，改为完整索引 + 每个判断能引用“哪页哪句” | 评审更准、可追溯 | 大 | ⏳ 未开始 | — |
| **P4 评估（eval）** | 自动量化“多智能体比单模型强多少”（证据覆盖、发现矛盾、引用正确率） | 能证明改进有没有用 | 中 | ⏳ 未开始 | — |
| **P5 研究员 subagents** | 评审前派“市场/技术/财务/团队/红队”查资料、核实事实，给 AI 投资人喂证据 | **质量最大提升** | **最大、最贵；此步才真需要 Temporal** | ⏳ 未开始 | — |
| **P6 审批 + 回放** | 草稿需你确认才生效；能回看“AI 为什么这么判、用了哪些证据” | 可信、可审计 | 中 | ⏳ 未开始 | — |
| （Shadow 双跑） | 新旧流程并跑对比后逐租户切换 | 迁移更稳 | 高（成本翻倍） | ⏸️ 建议 DEFER，等有真实流量 | — |

## 关键判断

1. **P2 是分水岭**：它是“崩了能自动恢复”，也是文档想上 Temporal 的真正理由。可先用**数据库**做到（把 Board 这类“等你回应”的模式拆成可分段恢复），不必引 Temporal，但是**真活儿**。
2. **P3 及以后偏“把产品做得更强”**（研究员查证据、可追溯），不再是修 bug；价值高，但工程量与模型调用成本也大。建议先与目标（老师/Codex）对齐再排。
3. **Temporal**：P0–P4 不引；到 P5 那种“多步骤、会失败、要恢复”的长任务再评估。

## 工作方式（约定）

- 每个改动走**分支 → PR → squash 合并**；不直推 `main`（推 `main` 即部署生产）。
- Railway **不自动迁移/seed**：改了 schema 要手动 `prisma db push`；改了预设角色要手动 `tsx prisma/seed.ts`（走生产库代理 URL）。
- 详细决策记录见 `AGENT-HARNESS-ARCHITECTURE-REVIEW.md` 的 Joint Decision Log 与 Claude Code Review 两节。
